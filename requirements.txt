# Core
numpy>=1.24
scipy>=1.11
pyyaml>=6.0
tqdm>=4.66

# Graph / verification
networkx>=3.2

# Generators run on CPU only — above is enough for steps 1-3.

# vLLM + HF (calibration on GPU server)
# pin to known-good versions for Qwen3 + AWQ
vllm>=0.6.3
torch>=2.4
transformers>=4.45
accelerate>=0.34
sentencepiece
tokenizers>=0.20

# ModelScope (魔塔) — used to download model weights from a domestic mirror
# that is much faster than HF in mainland China
modelscope>=1.18.0

# Stats / plotting (analysis only)
pandas>=2.1
matplotlib>=3.8

# MATH-500 official-style grading (EvalScope / Qwen2.5-Math lineage)
sympy>=1.12
latex2sympy2_extended>=1.0
regex>=2023.0
word2number>=1.1

# External benchmarks (GSM8K download)
datasets>=2.14
